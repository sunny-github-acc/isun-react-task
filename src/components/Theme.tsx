import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  typography: {
    body1: {
      fontWeight: "bold",
      fontSize: "inherit",
      color: "inherit",
      lineHeight: "inherit",
    },
  },
});

export default theme;
